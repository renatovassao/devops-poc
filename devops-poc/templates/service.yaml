apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-poc.fullname" . }}
  labels:
    {{- include "devops-poc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-poc.selectorLabels" . | nindent 4 }}
